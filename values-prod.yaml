replicaCount: 3

image:
  repository: nginx
  tag: "1.16.0"
  pullPolicy: IfNotPresent

config:
  appName: ecommerce-api
  environment: production
  logLevel: warn

secret:
  databasePassword: "prod-db-password"
  jwtSecret: "prod-jwt-secret"
  redisPassword: "prod-redis-password"
