replicaCount: 2

image:
  repository: nginx
  tag: "1.16.0"
  pullPolicy: IfNotPresent

config:
  appName: ecommerce-api
  environment: staging
  logLevel: info

secret:
  databasePassword: "staging-db-password"
  jwtSecret: "staging-jwt-secret"
  redisPassword: "staging-redis-password"
